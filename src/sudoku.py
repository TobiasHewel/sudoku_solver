"""Everything to do with handling the sudoku
"""


def solve(sudoku: tuple):
    """Solves the sudoku if possible

    Parameters:
    -------
        - sudoku (tuple[list[str]]): A two-dimentional list containing the start state of the sudoku

    Returns:
    -------
        - tuple[list[str]]: The solved version of the sudoku (returns None there's no solution)
    """
    return sudoku
